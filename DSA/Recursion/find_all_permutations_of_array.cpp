#include<bits/stdc++.h>
using namespace std;

void findAllPermutation(vector<int>&arr,vector<int>&ans,vector<int>ds,vector<int>freq)
{
`
}

int main()
{
    vector<int>arr = {1,2,3};
    vector<vector<int>>ans;
    vector<int>ds;
    vector<int>freq(arr.size(),0);
    findAllPermutation(arr,ans,ds,freq);

}