#include <bits/stdc++.h>
using namespace std;

int main() {
    // Write C++ code here
    vector<int>arr = {3,63,636,36,4,6,4};
    int target = 36;
    int size = arr.size();
    for(int i  = 0; i < size;i++)
        {
            if(arr[i] == target)
                return true;
        }
    cout << maximumElement;
    return 0;
}